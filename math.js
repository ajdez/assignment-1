function product(a, b) {
    if (a === 1) {
        return b;
    }
    else if (a === 2) {
        return b + b;
    }
    else {
        return "i can't do math that complex";
    }
}

function add (a,b){
    //hello
    return a+b;
}
